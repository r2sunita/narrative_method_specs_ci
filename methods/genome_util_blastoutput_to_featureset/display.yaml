#
# Define basic display information
#
name     : Build FeatureSet from BLAST Search
tooltip  : |
    Build FeatureSet using BLAST output based on filter parameters

screenshots :
    []

#
# Define the set of other narrative methods that should be suggested to the user.
#
suggestions :
    apps:
        related :
            []
        next :
            []
    methods:
        related :
            []
        next :
            []

#
# Configure the display and description of the parameters
#
parameters :
    in_id :
        ui-name : |
            BlastOutput object to build FeatureSet
        short-hint : |
            BlastOutput object to build FeatureSet
        long-hint  : |
            BlastOutput object to build FeatureSet
    out_id :
        ui-name : |
            FeatureSet name
        short-hint : |
            FeatureSet name
    evalue :
        ui-name : E-value
        short-hint : E-value cut off threshold. The statistical significance threshold for reporting FeatureSet based on BLAST output.
    entries :
        ui-name : Number of entries to keep
        short-hint : Restricts the number of features in the FeatureSet to the number specified that pass E-value threshold.


description : |
   Build FeatureSet using BLAST output based on the E-value and the number of features input by the user. 


